import type { NextConfig } from "next";

/**
 * Next.js yapılandırması.
 * Şu an özel bir ayara ihtiyaç yok; varsayılanlar Vercel deploy'u için yeterlidir.
 */
const nextConfig: NextConfig = {};

export default nextConfig;
